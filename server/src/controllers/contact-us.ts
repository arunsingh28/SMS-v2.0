import { Request, Response } from "express";
import _querySchema from "../models/contact.model";

const newQuery = (req: Request, res: Response) => {
  // save data to model
};

const module = { newQuery };

export default module;
